pragma solidity ^0.5.0;

contract TodoList { 
  uint public taskCount = 0; 
  // uint: state variable - written to the blockchain. state of blockchain changes whenever this does.
  // public means that we can read this from the task list
}